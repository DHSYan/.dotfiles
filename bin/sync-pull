#!/bin/sh

printf "\n==========================CONCEPT NOTES============================\n"
cd ~/Concept-Notes
git status
git fetch
git pull origin main

printf "\n\n========================NIXOS====================================\n"
cd ~/nixos
git status
git fetch
git pull origin main

printf "\n\n=========================DWM=====================================\n"
cd ~/dwm
git status
git fetch
git pull origin main

printf "\n\n========================DOTFILES=================================\n"
cd ~/.dotfiles
git status
git fetch
git pull origin main
